{{-- <div class="card mt-4">
    <div class="card-header">Team Performance</div>
    <div class="card-body">
        <ul class="list-group">
            @foreach($teamPerformance as $user)
                <li class="list-group-item">
                    <strong>{{ $user->name }}</strong>
                    - {{ $user->messages->count() }} issues assigned.
                </li>
            @endforeach
        </ul>
    </div>
</div> --}}
